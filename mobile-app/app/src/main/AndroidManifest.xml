<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.slide.healthai">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".UI.LoginActivity"
            android:exported="true"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".UI.PatientDetailsActivity"
            android:exported="false"
            android:label="Home"/>

        <activity
            android:name=".UI.PaymentActivity"
            android:exported="false"
            android:label="Payment"/>

        <activity
            android:name=".UI.ProfessionalHelpActivity"
            android:exported="false"
            android:label="Professional Help"/>

        <activity
            android:name=".UI.ReviewActivity"
            android:exported="false"
            android:label="Review"/>

        <activity
            android:name=".UI.SupportActivity"
            android:exported="false"
            android:label="Technical Support"/>

        <activity android:name=".UI.ChatActivity"
            android:exported="false"/>

        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />
    </application>
</manifest>
